# Schema

- `AuthToken` (class) in `api/auth.py:13` — fields: token, user_id, expires_at
- `AuthCredentials` (class) in `api/auth.py:20` — fields: username, password
- `WikiPage` (class) in `api/schema.py:6` — fields: id, title, content
- `CreatePageRequest` (class) in `api/schema.py:13` — fields: title, content
- `User` (class) in `api/schema.py:19` — fields: id, username, email
- `AppProps` (interface) in `web/src/App.tsx:4` — fields: title
- `LoginProps` (interface) in `web/src/auth/Login.tsx:3` — fields: onSuccess
